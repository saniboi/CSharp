namespace _01_Delegates
{
    // Delegates sind Typen wie Klassen; somit können sie ausserhalb
    // der Klasse "Program" oder in einer separaten Datei --wie andere
    // Klassen auch-- abgelegt werden.
    public delegate void WriteHandler(string word); // Schlüsselwort "delegate" 
}